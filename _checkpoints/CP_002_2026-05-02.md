# CHECKPOINT CP_002 — 2026-05-02

> Consolida lo trabajado en este hilo. Referencia base: CP_001 (pendiente subir).

---

## Estado general del proyecto

- Stack: Next.js 14 App Router + Supabase + Tailwind
- Repo: `redpatrimonio-v0.36` (rama `main`)
- Flujo central: Reporte → revisar → aprobar → publicar
- Roles activos con acceso dashboard: `partner`, `founder`

---

## Lo que existía antes de este hilo (CP_001 — pendiente formalizar)

### Flujo revisar
- `app/dashboard/revisar/page.tsx` — lista reportes en estado `rojo`
- `app/dashboard/revisar/[id]/page.tsx` — detalle + acción cambiar a amarillo

### Flujo aprobar
- `app/dashboard/aprobar/page.tsx` — lista reportes en estado `amarillo`
- `app/dashboard/aprobar/[id]/page.tsx` — detalle + acción publicar (estado → verde)

### Tabla principal
- `reportes_nuevos` con columnas: id_reporte, nombre_reporte, latitud, longitud,
  region, comuna, descripcion_ubicacion, categoria_general, tipologia_especifica,
  cultura_asociada, periodo_cronologico, estado_conservacion, nivel_proteccion,
  nivel_acceso, origen_acceso, nivel_accesibilidad, codigo_accesibilidad,
  amenazas, recinto_privado, estado_validacion, timestamp_creado, timestamp_publicacion,
  autor_reporte, id_usuario, telefono_usuario_contacto,
  contacto_propietario_posible, contacto_propietario_info,
  es_anonimo, autoriza_contacto, correo_usuario_contacto,
  temporalidad_riesgo, tipo_riesgo_principal, id_usuario_publico

### Tabla medios
- `reportes_medios`: id_medio, id_reporte, url_publica, descripcion_imagen,
  tipo_medio, prioridad_visualizacion

---

## Trabajo realizado en este hilo (CP_002)

### 1. Bloque contacto en `revisar/[id]/page.tsx`
- Se agregó sección **"👤 Quién reportó"** antes de la sección de ubicación
- Campos comunes (hallazgo y riesgo): `autor_reporte`, `telefono_usuario_contacto`,
  `contacto_propietario_posible` + `contacto_propietario_info`
- Campos exclusivos riesgo: `es_anonimo`, `autoriza_contacto`,
  `correo_usuario_contacto`, `temporalidad_riesgo`
- Banner rojo RIESGO ya existía; se verificó que permanece intacto
- Commit: feat(revisar): bloque contacto + campos riesgo en detalle revisar

### 2. Botón "Publicados" en `aprobar/page.tsx`
- Botón verde **"✓ Publicados"** agregado arriba a la derecha del header
- Navega a `/dashboard/publicados`
- El resto de la página quedó idéntico
- Commit: feat(dashboard): botón acceso a reportes publicados desde panel aprobar

### 3. Nuevo: `app/dashboard/publicados/page.tsx`
- Lista todos los reportes con `estado_validacion = 'verde'`
- Tabs: **Todos / Hallazgos / 🚨 Riesgo** con contadores en tiempo real
- Cards con: foto principal, nombre, badge rojo si es riesgo,
  región, comuna, fecha publicación, badge VERDE
- Click en card navega a `/dashboard/publicados/[id]`
- Protección de rol: solo `partner` y `founder`
- Commit: feat(dashboard): lista reportes publicados con tabs hallazgo/riesgo

### 4. Nuevo: `app/dashboard/publicados/[id]/page.tsx`
- Vista solo lectura del reporte publicado (no permite editar ni cambiar estado)
- Secciones:
  - Header: nombre, badges (VERDE / RIESGO / código), ID, fecha publicación
  - Botón **"📎 Generar denuncia CMN"** (riesgo) o **"📎 Generar ficha registro"** (hallazgo)
    → por ahora `alert()` placeholder, a implementar en próximo sprint
  - Banner rojo RIESGO con temporalidad
  - Bloque contacto (mismo diseño que revisar/[id])
  - Ubicación: región, comuna, coordenadas, recinto privado, descripción
  - Caracterización: categoría, tipología, cultura, periodo, conservación,
    protección, origen acceso, accesibilidad, amenazas/daño
  - Medios: collapsible, grid fotos + lista videos
- Componente auxiliar `<Field />` para campos solo lectura
- Commit: feat(dashboard): detalle reporte publicado — solo lectura + botón generar formulario

---

## Estructura de rutas dashboard (estado actual)

```
/dashboard
  /revisar          ← lista rojos
  /revisar/[id]     ← detalle + pasar a amarillo
  /aprobar          ← lista amarillos + botón → publicados
  /aprobar/[id]     ← detalle + publicar (pasar a verde)
  /publicados       ← lista verdes con tabs [NUEVO CP_002]
  /publicados/[id]  ← detalle solo lectura + generar formulario [NUEVO CP_002]
```

---

## Próximo sprint (pendiente)

1. **Generar denuncia CMN** (`/dashboard/publicados/[id]/denuncia`)
   - Formulario/PDF prefilled con datos del reporte de riesgo
   - Campos requeridos por CMN a definir

2. **Generar ficha de registro** (`/dashboard/publicados/[id]/ficha`)
   - Documento estándar para hallazgos
   - Formato a definir

3. **CP_001 formal** — subir checkpoint que consolide estado previo a este hilo

---

## Notas de arquitectura

- `categoria_general = 'arqueologia_en_riesgo'` es el discriminador hallazgo/riesgo
- `estado_validacion`: `rojo` → `amarillo` → `verde`
- `timestamp_publicacion` se setea al pasar a verde; puede ser null en registros viejos
- `codigo_accesibilidad` es el código público del hallazgo (solo hallazgos)
- `temporalidad_riesgo`: valores `pasado` / `activo` / `inminente`
- Medios: `prioridad_visualizacion` mayor = foto principal
- No hay paginación implementada aún en ninguna lista — a considerar cuando el volumen crezca
